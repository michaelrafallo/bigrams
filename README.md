# bigrams

A simple AI script that take a title from the database, eliminate noise words, take all possible word pairs of the title, and compare these pairs to those of every other title to see where the matches are, and list the clusters of titles. 
